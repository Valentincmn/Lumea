import React from "react";

export default function Hero() {
  return (
    <div>
      <section className="h-dvh w-screen overflow-hidden">
        <div className="h-full w-full flex items-center justify-center">content hero</div>
      </section>
    </div>
  );
}
